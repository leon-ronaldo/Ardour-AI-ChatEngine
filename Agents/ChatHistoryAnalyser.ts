import { Chat, Emotion } from "../models/ChatModels";
import { MemoryModel } from "../models/MemoryModel";
import Relationship from "../models/RelationShipModel";

class ChatHistoryAnalyser {

    chatHistory: Chat[];
    memory: MemoryModel;

    relationShipWithUser() {
        // let relationship: Relationship = {};

        // let yourAverageEmotion: Emotion;

        // let yourEmotions: Emotion[] = this.chatHistory
        //     .filter(chat => chat.sender === "AI")
        //     .map(chat => chat.emotion);

        // let userAverageEmotion: Emotion;

        // let userEmotions: Emotion[] = this.chatHistory
        //     .filter(chat => chat.sender === "USER")
        //     .map(chat => chat.emotion);


        // return relationship;
    }
}